# create a class called Isobel and initialise the class to take two arguments
# create an object of that class

class Isobel():
	def __init__(self, name, date):
		self.name = name
		self.date = date

instance = Isobel("Isobel", "6/4/1998")